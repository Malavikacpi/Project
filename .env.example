# Create a Neon Postgres database and add this value in Vercel Project Settings → Environment Variables.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Use a long, random value. It is required as a Bearer token for the private XLSX endpoint.
ADMIN_EXPORT_TOKEN=replace-with-a-long-random-secret
